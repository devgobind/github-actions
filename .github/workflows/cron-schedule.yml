name: Cron Schedule
on:
  schedule: 
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  Schedule_Test:
    runs-on: ubuntu-latest
    steps:
      - name: Schedule
        run: echo 'Schedule worked'
